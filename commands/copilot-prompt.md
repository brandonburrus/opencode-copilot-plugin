---
description: List or run a Copilot prompt file by name
---

Use the `copilot_prompt` tool to handle this request.

If `$ARGUMENTS` contains a prompt name, pass it as the `name` parameter.
If `$ARGUMENTS` is empty, call the tool with no `name` to list all available prompts.
